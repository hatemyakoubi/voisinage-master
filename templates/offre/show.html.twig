{% extends 'admin.html.twig' %}


{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ offre.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ offre.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ offre.createdAt ? offre.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Publier</th>
                <td>{{ offre.publier ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>
<div class="row" style="margin-top: 15px;">


    <a href="{{ path('offre_edit', {'id': offre.id}) }}"><button class="btn btn-success"style="margin-right: 15px;">Modifier</button></a>
    <a href="{{ path('offre_index') }}"class="btn btn-info" style="margin-right: 15px;">retour à la liste</a>

    {{ include('offre/_delete_form.html.twig') }}
</div>
{% endblock %}
