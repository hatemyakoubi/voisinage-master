{% extends 'base.html.twig' %}

{% block title %}UserImage{% endblock %}

{% block body %}
    <h1>UserImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_image.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ user_image.imageName }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ user_image.updatedAt ? user_image.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_image_index') }}">back to list</a>

    <a href="{{ path('user_image_edit', {'id': user_image.id}) }}">edit</a>

    {{ include('user_image/_delete_form.html.twig') }}
{% endblock %}
