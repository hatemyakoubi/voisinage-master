{% extends 'admin.html.twig' %}



{% block body %}
    <h1>SousCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_categorie.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ sous_categorie.title }}</td>
            </tr>
        </tbody>
    </table>



<a href="{{ path('sous_categorie_edit', {'id': sous_categorie.id}) }}" class="btn btn-warning" style="margin-bottom: 10px;">Modifier</a>
  {{ include('sous_categorie/_delete_form.html.twig') }}
<a href="{{ path('sous_categorie_index') }}" class="btn btn-info" style="margin-top: 10px;">retour à la liste</a>
{% endblock %}
